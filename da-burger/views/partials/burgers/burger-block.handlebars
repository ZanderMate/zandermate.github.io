<li>
	{{this.id}}
	{{this.burger_name}}

	{{#if devoured}}
	<button class="change-eaten" data-id="{{this.id}}" data-neweat="{{devoured}}">
		TIME TO EAT!
	</button>
	{{/if}}

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>