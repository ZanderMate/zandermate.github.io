<div class="jumbotron">
  <h1 class="text-center">Eat-Da-Burger!</h1>
  <img src="assets/img/burger.jfif" alt="Burger" class="mx-auto d-block" height="200px">
</div>

<div class="container">
  <div class="row">
    <div class="col-md-6" style="min-height: 100px;">
      <h3>Ready to Be Eaten:</h3>
      <ul class="align-middle" style="list-style-type: none;">
        {{#each burger}}
        {{#unless devoured}}
        {{> burgers/burger-block}}
        {{/unless}}
        {{/each}}
      </ul>
    </div>
    <div class="col-md-6">
      <h3 style="text-align: right;">Already Eaten:</h3>
      <ul style="list-style-type: none;">
        {{#each burger}}
        {{#if devoured}}
        {{> burgers/burger-block}}
        {{/if}}
        {{/each}}
      </ul4>
    </div>
  </div>
</div>

<form class="create-form text-center">
  <textarea id="burger_name" class="mx-auto d-block" cols="50" rows="5"></textarea>
  <button class="btn btn-primary submit-button" type="submit">Submit</button>
</form>